{
  "index_patterns": ["{{ item.name }}*"],
  "template": {
      "settings": {
        "number_of_shards": {{ item.shards_number }},
        "number_of_replicas": {{ item.replicas_number }},
        "index.lifecycle.name": "{{ item.policy }}"
    }
  }
}